#include <iostream>

#define LES71(x, y) ((0 <= (x) && (x < y)) ? 1 : 0)
#define SIZE 4
#define LES72(arr, a) std::cout << *arr[a] <<std::endl;


int main()
{
/*
    int x, y;
    std::cout << " input X: ";
    std::cin >> x;
    std::cout << " input Y: ";
    std::cin >> y;
    if ((LES71(x, y)) == 1)
        std::cout << "Yes";
    else
        std::cout << "No";
*/

/*
    int a = 3;
    int arr[SIZE];
    arr[0] = 10;
    arr[1] = 5;
    arr[3] = 24;
    LES72(&arr, a)
*/





    return 0;
}
